import { BadRequestException, ForbiddenException } from '@nestjs/common'; import { BudgetService } from '../src/budget/budget.service'; describe('BudgetService security and ledger',()=>{const prisma:any={budgetPocket:{findUnique:jest.fn()},budgetTransaction:{findMany:jest.fn(),create:jest.fn()},$transaction:jest.fn(async(fn:any)=>fn({budgetTransaction:{create:prisma.budgetTransaction.create},auditLog:{create:jest.fn()}}))};beforeEach(()=>jest.clearAllMocks());it('denies a school user from another school',async()=>{prisma.budgetPocket.findUnique.mockResolvedValue({id:'p1',budgetPool:{schoolId:'school-b'}});const s=new BudgetService(prisma);await expect(s.balance({id:'u',organizationId:'o',schoolId:'school-a',roles:[]},'p1')).rejects.toBeInstanceOf(ForbiddenException)});it('prevents insufficient deductions',async()=>{prisma.budgetPocket.findUnique.mockResolvedValue({id:'p1',budgetPool:{schoolId:'school-a'}});prisma.budgetTransaction.findMany.mockResolvedValue([{type:'OPENING',amount:100}]);const s=new BudgetService(prisma);await expect(s.post({id:'u',organizationId:'o',schoolId:'school-a',roles:[]},{budgetPocketId:'p1',type:'EXPENSE',amount:101} as any)).rejects.toBeInstanceOf(BadRequestException);expect(prisma.budgetTransaction.create).not.toHaveBeenCalled()})});
