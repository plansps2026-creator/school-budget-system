import { calculateLedger, assertSufficientBudget } from '../src/common/domain/budget-ledger'; import { calculateFree15 } from '../src/common/domain/free15'; import { nextWorkflow } from '../src/common/domain/workflow'; import { isAiActionAllowed } from '../src/common/domain/ai-guardrails'; describe('budget ledger domain',()=>{it('calculates ledger',()=>expect(calculateLedger([{type:'OPENING',amount:1000},{type:'ALLOCATION',amount:500},{type:'COMMITMENT',amount:200},{type:'EXPENSE',amount:100},{type:'TRANSFER_OUT',amount:50},{type:'REVERSAL',amount:25}])).toEqual({available:1175,commitments:200,actualExpenses:100}));it('rejects spending above available balance',()=>expect(()=>assertSufficientBudget(100,101)).toThrow('Insufficient available budget'))});describe('free15 domain',()=>{it('calculates deterministically',()=>{const r=calculateFree15([{category:'TEXTBOOK',levelCode:'P1',studentCount:10,rate:200},{category:'TEXTBOOK',levelCode:'P2',studentCount:5,rate:300},{category:'UNIFORM',levelCode:'P1',studentCount:0,rate:500}]);expect(r.items.map(x=>x.amount)).toEqual([2000,1500]);expect(r.total).toBe(3500)})});describe('workflow domain',()=>{it('advances and closes',()=>{expect(nextWorkflow(1,'APPROVE')).toEqual({status:'PENDING',currentStep:2});expect(nextWorkflow(4,'APPROVE')).toEqual({status:'APPROVED',currentStep:5});expect(nextWorkflow(2,'RETURN')).toEqual({status:'RETURNED',currentStep:2});expect(nextWorkflow(2,'REJECT')).toEqual({status:'REJECTED',currentStep:2})})});describe('AI guardrails',()=>{it('allows analysis/drafting and blocks high-impact actions',()=>{expect(isAiActionAllowed('ANALYZE')).toBe(true);expect(isAiActionAllowed('DRAFT')).toBe(true);expect(isAiActionAllowed('APPROVE')).toBe(false);expect(isAiActionAllowed('PAY')).toBe(false)})});
