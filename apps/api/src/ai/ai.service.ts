import{createHash}from'crypto';import{AI_ALLOWED,AI_FORBIDDEN}from'../common/domain/ai-guardrails';import{Injectable}from'@nestjs/common';import{PrismaService}from'../prisma/prisma.service';@Injectable()export class AiService{constructor(private p:PrismaService){}guardrails(){return{allowed:[...AI_ALLOWED],forbidden:[...AI_FORBIDDEN]}}log(u:any,d:any){const h=createHash('sha256').update(JSON.stringify(d.input||'')).digest('hex');return this.p.aiAuditLog.create({data:{organizationId:u.organizationId,schoolId:u.schoolId,actorUserId:u.id,task:d.task,inputHash:h,outputSummary:d.outputSummary,modelVersion:d.modelVersion,sourceRefs:d.sourceRefs,risk:d.risk||'LOW',humanConfirmed:!!d.humanConfirmed}})}}
