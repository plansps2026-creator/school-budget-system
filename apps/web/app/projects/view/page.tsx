'use client';
import { useEffect,useState } from 'react';import { apiFetch } from '../../../lib/api';
type Project={id:string;code:string;name:string;status:string;approvedBudget:string;school:{name:string};fiscalYear:{yearBE:number};activities:{id:string;name:string;status:string;allocatedBudget:string;expenses:{id:string;amount:string;description:string}[]}[]};
export default function ProjectView(){const [p,setP]=useState<Project|null>(null);const [msg,setMsg]=useState('');const id=typeof window!=='undefined'?new URLSearchParams(location.search).get('id'):null;useEffect(()=>{if(id)apiFetch<Project>(`/projects/${id}`).then(setP).catch(e=>setMsg(e.message))},[id]);if(!id)return <div className="page-stack"><section className="card"><h1>ไม่พบ project id</h1></section></div>;if(msg)return <div className="page-stack"><section className="card"><h1>โหลดไม่สำเร็จ</h1><p>{msg}</p></section></div>;if(!p)return <div className="page-stack"><section className="card"><p>กำลังโหลด…</p></section></div>;return <div className="page-stack"><div className="page-header"><div><h1>{p.code} — {p.name}</h1><p className="muted">{p.school.name} · ปีงบประมาณ {p.fiscalYear.yearBE}</p></div><span className="status">{p.status}</span></div><div className="grid"><div className="card"><div className="muted">วงเงินอนุมัติ</div><div className="metric">{p.approvedBudget}</div></div><div className="card"><div className="muted">กิจกรรม</div><div className="metric">{p.activities.length}</div></div></div><section className="card"><h2>Activities</h2>{p.activities.length===0?<p className="muted">ยังไม่มีกิจกรรม</p>:<div className="table-wrap"><table><thead><tr><th>กิจกรรม</th><th>สถานะ</th><th>งบ</th><th>ค่าใช้จ่าย</th><th></th></tr></thead><tbody>{p.activities.map(a=><tr key={a.id}><td>{a.name}</td><td>{a.status}</td><td>{a.allocatedBudget}</td><td>{a.expenses.reduce((s,e)=>s+Number(e.amount),0).toFixed(2)}</td><td><a href={`/expenses?activityId=${encodeURIComponent(a.id)}`}>ค่าใช้จ่าย</a></td></tr>)}</tbody></table></div>}</section></div>}
