'use client';
import {FormEvent,useState} from 'react';
export default function Login(){const[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000/api'}/auth/login`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const d=await r.json();if(r.ok){localStorage.setItem('token',d.accessToken);location.href='/dashboard'}else setMsg(d.message||'เข้าสู่ระบบไม่สำเร็จ')}return <main className="card" style={{maxWidth:460}}><h1>เข้าสู่ระบบ</h1><form onSubmit={submit} style={{display:'grid',gap:12}}><input name="email" type="email" defaultValue="admin@school.local"/><input name="password" type="password" defaultValue="ChangeMe123!"/><button>เข้าสู่ระบบ</button></form><p>{msg}</p></main>}
