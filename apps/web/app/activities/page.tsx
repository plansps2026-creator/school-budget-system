'use client';
import { FormEvent,useEffect,useState } from 'react';import { apiFetch } from '../../lib/api';
export default function Activities(){const [items,setItems]=useState<any[]>([]);const [msg,setMsg]=useState('');const q=typeof window!=='undefined'?new URLSearchParams(location.search).get('projectId'):null;async function load(){try{setItems(await apiFetch<any[]>(`/activities${q?`?projectId=${encodeURIComponent(q)}`:''}`))}catch(e:any){setMsg(e.message)}}useEffect(()=>{load()},[q]);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await apiFetch('/activities',{method:'POST',body:JSON.stringify({projectId:f.get('projectId'),name:f.get('name'),allocatedBudget:Number(f.get('allocatedBudget')||0)})});e.currentTarget.reset();load()}catch(e:any){setMsg(e.message)}}return <div className="page-stack"><div className="page-header"><h1>กิจกรรม</h1></div><section className="card"><h2>เพิ่มกิจกรรม</h2><form onSubmit={create} className="form-grid"><input name="projectId" defaultValue={q||''} placeholder="projectId" required/><input name="name" placeholder="ชื่อกิจกรรม" required/><input name="allocatedBudget" type="number" min="0" step="0.01" placeholder="งบกิจกรรม"/><button className="btn btn-primary btn-md">บันทึก</button></form>{msg&&<p>{msg}</p>}</section><section className="card"><h2>รายการ</h2>{items.map(a=><div className="list-row" key={a.id}><div><b>{a.name}</b><div className="muted">{a.project?.code} · {a.status}</div></div><a href={`/expenses?activityId=${encodeURIComponent(a.id)}`}>จัดการค่าใช้จ่าย</a></div>)}</section></div>}
